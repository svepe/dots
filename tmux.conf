# remap prefix from 'C-b' to 'C-a'
set-option -g prefix C-a
bind-key C-a send-prefix
unbind C-b

# split panes using '-' and '='
bind - split-window -h
bind = split-window -v
unbind '"'
unbind %

# reload config file with 'r'
bind r source-file ~/.tmux.conf

# switch panes using 'Alt-arrow' without prefix
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# do not rename windows automatically
# Enable mouse control
set -g mouse 

# Adjust the color of the pane border such that the active pane is brighter
set -g pane-border-fg green
set -g pane-border-bg default
set -g pane-active-border-fg brightgreen
set -g pane-active-border-bg default

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-sessionist'


set -g @continuum-save-interval '60'

# set -g status-right  '#(/home/svetlin/.tmux/plugins/tmux-continuum/scripts/continuum_save.sh) #{prefix_highlight} "#{=21:pane_title}" %H:%M %d-%b-%y'

source-file '/home/svetlin/.tmuxline.conf'

run '~/.tmux/plugins/tpm/tpm'

